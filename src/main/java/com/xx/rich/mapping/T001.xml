<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xx_T001"> 
	
	 <parameterMap type="java.util.HashMap" id="paraMap" />

	<!--获取[职员]列表 -->
	<select id="t001_page" parameterMap="paraMap" resultType="hashmap">
		<if test="start != null"><![CDATA[select t.*  from ( ]]></if>
		select
		*
		from rich_T001
		<if test="filter != null">
			<where>${filter}</where>
		</if>
		<if test="sort != null">ORDER BY ${sort} ${dir}</if>
		<if test="sort == null">ORDER BY ID DESC</if>
		<if test="start != null"> <![CDATA[) t limit ${start},${limit}]]></if>
	</select>

	<!--获取[职员]列表总数 -->
	<select id="t001_page_count" parameterMap="paraMap" resultType="int">
		select count(1) from rich_T001 a
		<if test="filter != null">
			<where>${filter}</where>
		</if>
	</select>
<!-- 	<xx_type id="xx_id" parameterMap="paraMap" xx_resultType > -->
<!-- 		xx_sql -->
<!-- 	</xx_type> -->

<!-- 	<xx_type id="xx_id" parameterMap="paraMap" xx_resultType > -->
<!-- 		xx_sql -->
<!-- 	</xx_type> -->

<!-- 	<xx_type id="xx_id" parameterMap="paraMap" xx_resultType > -->
<!-- 		xx_sql -->
<!-- 	</xx_type> -->
</mapper>
